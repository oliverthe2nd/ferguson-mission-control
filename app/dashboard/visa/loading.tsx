import { DashboardChartsSkeleton } from "@/components/ui/dashboard-skeleton";

export default function VisaLoading() {
  return <DashboardChartsSkeleton columns={2} chartCount={3} tallIndex={1} />;
}
